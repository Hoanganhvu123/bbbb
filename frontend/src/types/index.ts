export interface FoodItem {
  id: number;
  title: string;
  description: string;
  price: string;
  imageURL: string;
  category: string;
}

export interface cartItem {
  id: number;
  title: string;
  description: string;
  price: string;
  imageURL: string;
  category: string;
  qty: number;
} 